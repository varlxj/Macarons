$(document).ready(function($){
	$(window).resize((function(){
		var height = $(document).height() - $(".navibar").outerHeight();
		$("#left,#right").height(height);
		return arguments.calee
	})());
});